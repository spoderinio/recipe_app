# 📖 Книга с рецепти - Web приложение

Web базирана система за управление и търсене на рецепти с администраторски панел.

## ✨ Функционалности

### Потребителски интерфейс:
- 🔍 **Търсене на рецепти** - търсене по име или съставки (в реално време)
- 📱 **Mobile-friendly дизайн** - оптимизиран за Android таблети и телефони
- 🌓 **Светла и тъмна тема** - автоматично запазване на предпочитанията
- 🔊 **Text-to-Speech** - изслушване на стъпките за приготвяне (опционално)
- 📖 **Преглед на рецепти** - детайлна информация за всяка рецепта

### Администраторски панел:
- ➕ **Добавяне на рецепти** - лесно добавяне на нови рецепти
- ✏️ **Редактиране** - промяна на съществуващи рецепти
- 🗑️ **Изтриване** - премахване на рецепти
- 📋 **Списък с всички рецепти** - бърз достъп до всички рецепти

## 🛠️ Технологии

- **Backend**: Python 3 + Flask
- **База данни**: SQLite
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **TTS**: Web Speech API

## 📦 Инсталация

### 1. Изтегляне на файловете

Копирайте всички файлове в директория на Raspberry Pi (напр. `/home/pi/recipe_app`)

### 2. Създаване на Virtual Environment

```bash
cd /home/pi/recipe_app

# Създаване на virtual environment
python3 -m venv venv

# Активиране
source venv/bin/activate

# Инсталация на зависимости
pip install -r requirements.txt
```

**Защо Virtual Environment?**
- Изолира Python пакетите от системните
- Предотвратява конфликти с други приложения
- Позволява различни версии на библиотеки
- Препоръчва се за production deployment

### 3. Инициализация на базата данни

```bash
# С активиран virtual environment
python add_sample_recipes.py

# Или директно
/home/pi/recipe_app/venv/bin/python /home/pi/recipe_app/add_sample_recipes.py
```

### 4. Стартиране на приложението

За тестване:
```bash
# Активирай venv
source venv/bin/activate

# Стартирай
python app.py

# Или директно
/home/pi/recipe_app/venv/bin/python /home/pi/recipe_app/app.py
```

За production (с системен сервиз):
```bash
# Създаване на systemd service файл
sudo cp recipe_app.service /etc/systemd/system/
```

Съдържание на service файла (вече конфигуриран за venv):
```ini
[Unit]
Description=Recipe App
After=network.target

[Service]
User=pi
WorkingDirectory=/home/pi/recipe_app
ExecStart=/home/pi/recipe_app/venv/bin/python /home/pi/recipe_app/app.py
Restart=always
RestartSec=10
Environment="PYTHONUNBUFFERED=1"

[Install]
WantedBy=multi-user.target
```

Активиране на сервиза:
```bash
sudo systemctl daemon-reload
sudo systemctl enable recipe_app
sudo systemctl start recipe_app
sudo systemctl status recipe_app
```

## 🌐 Достъп до приложението

### Локална мрежа:
```
http://[IP_НА_RASPBERRY_PI]:5002
```

Намиране на IP адреса:
```bash
hostname -I
```

### Достъп от интернет (опционално):

**⚠️ За интернет достъп е необходимо:**

1. **HTTPS криптиране** - използване на обратен прокси (nginx) с SSL сертификат
2. **Автентикация** - добавяне на потребителско име и парола за админ панела
3. **Firewall конфигурация** - отваряне на порт в рутера

#### Пример за nginx конфигурация с SSL:

```bash
sudo apt-get install nginx certbot python3-certbot-nginx
```

Nginx конфигурация (`/etc/nginx/sites-available/recipe_app`):
```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;
    
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    location / {
        proxy_pass http://127.0.0.1:5002;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Активиране:
```bash
sudo ln -s /etc/nginx/sites-available/recipe_app /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
sudo certbot --nginx -d your-domain.com
```

## 📱 Използване

### За потребители:
1. Отворете браузъра на телефона/таблета
2. Навигирайте до `http://[IP]:5002`
3. Използвайте търсачката за намиране на рецепти
4. Кликнете на рецепта за детайли
5. Натиснете 🔊 бутона за Text-to-Speech

### За администратори:
1. Отворете `/admin` от лаптоп или таблет
2. Попълнете формата с име, съставки и начин на приготвяне
3. Натиснете "Добави рецепта"
4. Използвайте бутоните "Редактирай" или "Изтрий" за управление

## 🎨 Теми

Приложението поддържа светла и тъмна тема:
- Кликнете на 🌙/☀️ бутона в горния десен ъгъл
- Предпочитанията се запазват автоматично

## 🔊 Text-to-Speech

TTS е налично само ако браузърът го поддържа:
- ✅ Chrome/Edge на Android
- ✅ Safari на iOS
- ✅ Chrome/Edge/Firefox на Windows/Linux

За най-добър резултат на български език:
- Android: инсталирайте български глас от настройките на системата
- Windows: инсталирайте български език пакет

## 🗂️ Структура на базата данни

```sql
CREATE TABLE recipes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    ingredients TEXT NOT NULL,
    instructions TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 🔧 Допълнителни настройки

### Промяна на порта:
Редактирайте `app.py`, ред:
```python
app.run(debug=True, host='0.0.0.0', port=5002)
```

### Backup на базата данни:
```bash
cp recipes.db recipes_backup_$(date +%Y%m%d).db
```

### Възстановяване от backup:
```bash
cp recipes_backup_YYYYMMDD.db recipes.db
```

## 📝 API Endpoints

### GET `/api/search?q=query`
Търсене на рецепти

### GET `/api/recipe/<id>`
Получаване на конкретна рецепта

### POST `/api/recipe`
Добавяне на нова рецепта
```json
{
  "name": "Име на рецептата",
  "ingredients": "Съставки",
  "instructions": "Начин на приготвяне"
}
```

### PUT `/api/recipe/<id>`
Обновяване на рецепта

### DELETE `/api/recipe/<id>`
Изтриване на рецепта

## 🐛 Отстраняване на проблеми

### Приложението не стартира:
```bash
# Проверка на портове
sudo netstat -tulpn | grep 5002

# Проверка на логове
sudo journalctl -u recipe_app -f
```

### Базата данни е повредена:
```bash
# Създаване на нова база данни
rm recipes.db
python3 database.py
```

### TTS не работи:
- Проверете дали браузърът поддържа Web Speech API
- Уверете се, че има инсталиран български глас
- Проверете настройките за звук на устройството

## 📄 Лиценз

Това приложение е разработено за лично ползване.

## 👨‍💻 Автор

Разработено за Raspberry Pi home server
Заедно с Norsk Drill и Fuel Consumption приложенията
