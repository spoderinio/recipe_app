# 📖 КНИГА С РЕЦЕПТИ - ОБОБЩЕНИЕ НА ПРОЕКТА

## 🎯 Завършен проект готов за deployment на Raspberry Pi

---

## 📋 КАКВО Е СЪЗДАДЕНО

Web базирана система за управление и търсене на рецепти с пълна функционалност.

### ✅ ОСНОВНИ ФУНКЦИИ:
- 🔍 **Търсене в реално време** - търси по име или съставки
- 📱 **Mobile-friendly** - оптимизирано за Android таблети и телефони  
- 💻 **Desktop-friendly** - работи отлично и на Windows лаптоп
- 🌓 **Светла/Тъмна тема** - автоматично запазване
- 🔊 **Text-to-Speech** - четене на инструкциите (опционално)
- ⚙️ **Администраторски панел** - добавяне, редактиране, изтриване
- 💾 **SQLite база данни** - бързо и ефективно съхранение

---

## 📁 СТРУКТУРА НА ПРОЕКТА

```
recipe_app/
├── app.py                      # Flask сървър (главно приложение)
├── database.py                 # SQLite операции
├── requirements.txt            # Python зависимости
├── recipe_app.service          # Systemd конфигурация
├── add_sample_recipes.py       # Скрипт за примерни рецепти
├── setup.sh                    # Автоматичен setup скрипт
├── .gitignore                  # Git ignore файл
├── README.md                   # Подробна документация
├── INSTALL.md                  # Стъпка-по-стъпка инструкции
│
├── venv/                       # Virtual environment (създава се при setup)
│   ├── bin/                    # Python изпълними файлове
│   ├── lib/                    # Инсталирани пакети
│   └── ...
│
├── static/
│   ├── css/
│   │   └── style.css          # Стилове (светла/тъмна тема)
│   └── js/
│       └── app.js             # JavaScript (търсене, TTS, админ)
│
└── templates/
    ├── index.html             # Главна страница (списък + търсене)
    ├── recipe.html            # Преглед на рецепта
    └── admin.html             # Администраторски панел
```

**Забележка:** `venv/` директорията се създава автоматично при setup и съдържа изолирани Python пакети.

---

## 🚀 БЪРЗ СТАРТ

### ⚡ Автоматична инсталация (ПРЕПОРЪЧИТЕЛНО)

```bash
cd /home/pi/recipe_app
bash setup.sh
```

Скриптът автоматично ще направи всичко за вас!

### 📖 Ръчна инсталация

### 1️⃣ Копиране на файловете
```bash
# На Raspberry Pi:
cd /home/pi
# Копирайте директорията recipe_app
```

### 2️⃣ Инсталация с Virtual Environment
```bash
cd /home/pi/recipe_app

# Създаване на virtual environment
python3 -m venv venv

# Активиране
source venv/bin/activate

# Инсталация на зависимости
pip install -r requirements.txt

# Добавяне на примерни рецепти
python add_sample_recipes.py
```

**⚠️ ВАЖНО:** За production deployment virtual environment е задължителен!

### 3️⃣ Стартиране
```bash
# Ако venv е активиран
python app.py

# Или директно
/home/pi/recipe_app/venv/bin/python /home/pi/recipe_app/app.py
```

### 4️⃣ Достъп
```
http://[IP_НА_RASPBERRY_PI]:5002
```

Намиране на IP:
```bash
hostname -I
```

---

## 🔧 АВТОМАТИЧНО СТАРТИРАНЕ (SYSTEMD)

**⚠️ Първо създайте virtual environment и инсталирайте зависимостите!**

```bash
# Копиране на service файла (вече конфигуриран за venv)
sudo cp recipe_app.service /etc/systemd/system/

# Активиране
sudo systemctl enable recipe_app
sudo systemctl start recipe_app

# Проверка
sudo systemctl status recipe_app
```

**Service файлът използва:** `/home/pi/recipe_app/venv/bin/python`

---

## 📱 ИЗПОЛЗВАНЕ

### За потребители (телефон/таблет):
1. Отвори `http://[IP]:5002`
2. Използвай търсачката за намиране на рецепти
3. Кликни на рецепта за детайли
4. Натисни 🔊 за Text-to-Speech

### За администратори (лаптоп):
1. Отвори `http://[IP]:5002/admin`
2. Попълни формата с нова рецепта
3. Използвай бутоните за редактиране/изтриване

---

## 🎨 ФУНКЦИОНАЛНОСТИ

### ✨ Търсене:
- Търси в реално време (debounce 300ms)
- Търси по име НА рецептата
- Търси в съставките
- Моментално показване на резултати

### 🌓 Теми:
- Светла тема (по подразбиране)
- Тъмна тема
- Автоматично запазване в localStorage
- Бутон за превключване в header-а

### 🔊 Text-to-Speech:
- Изчита инструкциите на български
- Автоматично търси български глас
- Бутон за пауза/спиране
- Визуална индикация при четене
- Скрива се ако браузърът не поддържа

### ⚙️ Администрация:
- Добавяне на рецепти (име, съставки, инструкции)
- Редактиране на съществуващи рецепти
- Изтриване с потвърждение
- Списък с всички рецепти
- Препоръка за въвеждане: 
  * Съставки - на нови редове или разделени със запетая
  * Инструкции - номерирани стъпки

---

## 💾 БАЗА ДАННИ

### Схема:
```sql
CREATE TABLE recipes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,              -- Име на рецептата
    ingredients TEXT NOT NULL,        -- Съставки
    instructions TEXT NOT NULL,       -- Начин на приготвяне
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Размер:
- Празна база: ~3-5 KB
- С 5 рецепти: ~20 KB
- С 100 рецепти: ~100-200 KB
- С 1000 рецепти: ~1-2 MB

Изключително ефективно за Raspberry Pi!

---

## 🌐 ПОРТОВЕ И МРЕЖА

### По подразбиране:
- **Порт**: 5002
- **Host**: 0.0.0.0 (достъпен от мрежата)

### Промяна на порт:
Редактирай `app.py`, последен ред:
```python
app.run(debug=True, host='0.0.0.0', port=8080)  # Нов порт
```

---

## 🔐 СИГУРНОСТ

### Локална мрежа (текуща конфигурация):
✅ Безопасна - достъпна само от вашата домашна мрежа

### За интернет достъп (бъдеща опция):
⚠️ ЗАДЪЛЖИТЕЛНО използвай:
- **HTTPS** - SSL/TLS криптиране (nginx + Let's Encrypt)
- **Парола** - HTTP Basic Auth или Flask-Login
- **Firewall** - Правилна конфигурация

📖 Виж README.md раздел "Достъп от интернет" за детайли

---

## 🔄 BACKUP & RESTORE

### Ръчен backup:
```bash
cp recipes.db backup_$(date +%Y%m%d).db
```

### Автоматичен ежедневен backup:
```bash
crontab -e
# Добави:
0 2 * * * cp /home/pi/recipe_app/recipes.db /home/pi/recipe_app/backup_$(date +\%Y\%m\%d).db
```

### Възстановяване:
```bash
sudo systemctl stop recipe_app
cp backup_YYYYMMDD.db recipes.db
sudo systemctl start recipe_app
```

---

## 📊 API ENDPOINTS

Приложението има RESTful API:

- `GET /api/search?q=query` - Търсене
- `GET /api/recipe/<id>` - Получаване на рецепта
- `POST /api/recipe` - Добавяне на рецепта
- `PUT /api/recipe/<id>` - Обновяване на рецепта
- `DELETE /api/recipe/<id>` - Изтриване на рецепта

---

## 🎨 ДИЗАЙН

### Цветови палитри:

**Светла тема:**
- Фон: #ffffff (бяло)
- Текст: #333333 (тъмносиво)
- Карти: #f5f5f5 (светлосиво)
- Акцент: #4CAF50 (зелено)

**Тъмна тема:**
- Фон: #1a1a1a (тъмно)
- Текст: #e0e0e0 (светлосиво)
- Карти: #2d2d2d (сиво)
- Акцент: #66BB6A (светлозелено)

### Responsive breakpoints:
- Mobile: < 768px
- Desktop: ≥ 768px

---

## 🐛 ОТСТРАНЯВАНЕ НА ПРОБЛЕМИ

### Порт 5002 зает:
```bash
sudo netstat -tulpn | grep 5002
# Или промени порта в app.py
```

### Приложението не стартира:
```bash
sudo journalctl -u recipe_app -n 50
cd /home/pi/recipe_app && python3 app.py
```

### Няма достъп от други устройства:
```bash
# Провери IP адреса
hostname -I

# Провери firewall
sudo ufw status
sudo ufw allow 5002  # ако е необходимо
```

### TTS не работи:
- Провери дали браузърът поддържа Web Speech API
- Инсталирай български глас в Android настройки
- Провери звуковите настройки на устройството

---

## 📦 ВКЛЮЧЕНИ ПРИМЕРНИ РЕЦЕПТИ

Скриптът `add_sample_recipes.py` добавя 5 традиционни български рецепти:

1. 🥐 **Баница със сирене**
2. 🥗 **Шопска салата**
3. 🍲 **Пилешка супа**
4. 🥒 **Тиквички с ориз**
5. 🍖 **Мусака с картофи**

Отличен начин за тестване на приложението!

---

## 🔮 БЪДЕЩИ ПОДОБРЕНИЯ (ОПЦИОНАЛНИ)

Идеи за разширяване:
- 📷 Снимки на рецепти
- 🏷️ Категории и тагове
- ⭐ Рейтинг система
- 💬 Коментари
- 🔗 Споделяне на рецепти
- 📊 Калкулатор на калории
- 🛒 Списък за пазаруване
- 👥 Многопотребителска система
- 🌍 Многоезичност

---

## 📞 СЪВМЕСТИМОСТ

### Браузъри:
- ✅ Chrome/Edge (Android, Windows, Linux)
- ✅ Firefox (Android, Windows, Linux)
- ✅ Safari (iOS, macOS)
- ✅ Samsung Internet (Android)

### Операционни системи:
- ✅ Android 5.0+
- ✅ iOS 12+
- ✅ Windows 7+
- ✅ Linux (всички дистрибуции)
- ✅ macOS 10.12+

### Резолюции:
- ✅ Телефони (320px+)
- ✅ Таблети (768px+)
- ✅ Лаптопи (1024px+)
- ✅ Desktop (1920px+)

---

## 📚 ДОКУМЕНТАЦИЯ

Създадените документи:
- **PROJECT_OVERVIEW.md** - Това ръководство (кратко обобщение)
- **README.md** - Пълна документация (7.8 KB)
- **INSTALL.md** - Стъпка-по-стъпка инструкции (актуализирано с venv)
- **VENV_GUIDE.md** - Подробно ръководство за Virtual Environment
- **setup.sh** - Автоматизиран инсталационен скрипт

---

## ✅ ГОТОВНОСТ ЗА PRODUCTION

Приложението е:
- ✅ Напълно функционално
- ✅ Тествано с примерни данни
- ✅ Оптимизирано за мобилни устройства
- ✅ С документация на български
- ✅ Готово за deployment на Raspberry Pi
- ✅ Съвместимо с Norsk Drill и Fuel Consumption

---

## 🎉 ЗАКЛЮЧЕНИЕ

Приложението е **готово за използване** на твоя Raspberry Pi сървър!

Всички файлове са налични в директорията `recipe_app/`.

Следвай инструкциите в `INSTALL.md` за бърза инсталация.

**Приятно готвене! 👨‍🍳**

---

*Разработено: Януари 2026*
*За: Raspberry Pi Home Server*
*Заедно с: Norsk Drill, Fuel Consumption*
